include ${PETSC_DIR}/lib/petsc/conf/variables
include ${PETSC_DIR}/lib/petsc/conf/rules

#Intel compiler
FC = mpifort
CC = icc
#flags for intel compiler
#FFLAGS = -O3 -ffree-line-length-512 -I$(PETSC_DIR)/include

#gcc compiler
#FC = gfortran
#CC = gcc
#flags for gcc(profiling)
#FFLAGS =-march=native -O3 -pg 
#flags for gcc
#FFLAGS =-march=native -O3 -ffree-line-length-512 

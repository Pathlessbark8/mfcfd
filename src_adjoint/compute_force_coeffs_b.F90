!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.14 (r7079) -  5 Oct 2018 09:56
!
MODULE COMPUTE_FORCE_COEFFS_MOD_DIFF
#include <petsc/finclude/petscsys.h>

  USE DATA_STRUCTURE_MOD_DIFF
  USE PETSC_DATA_STRUCTURE_MOD
  IMPLICIT NONE

CONTAINS
!  Differentiation of compute_cl_cd_cm in reverse (adjoint) mode (with options fixinterface):
!   gradient     of useful results: clcd cd cl cm point.x point.y
!                point.nx point.ny point.prim
!   with respect to varying inputs: point.x point.y point.nx point.ny
!                point.prim
  SUBROUTINE COMPUTE_CL_CD_CM_B()
    IMPLICIT NONE
    INTEGER :: i, j, k
    INTEGER :: l, m, r
    REAL*8 :: cp, temp
    REAL*8 :: cpb
    REAL*8 :: lx, ly, mx, my, rx, ry
    REAL*8 :: lxb, lyb, mxb, myb, rxb, ryb
    REAL*8 :: ds1, ds2, ds
    REAL*8 :: ds1b, ds2b, dsb
    REAL*8, DIMENSION(shapes) :: h, v, pitch_mom, lcl, lcd
    REAL*8, DIMENSION(shapes) :: hb, vb, pitch_momb
    REAL*8 :: nx, ny
    REAL*8 :: nxb, nyb
    INTRINSIC DSQRT
    INTRINSIC DCOS
    INTRINSIC DSIN
    REAL*8 :: tempb
    REAL*8 :: tempb0
    REAL*8 :: tempb1
    REAL*8 :: tempb2
    REAL*8 :: tempb3
    REAL*8 :: tempb4
    REAL*8 :: tempb5
    REAL*8 :: tempb6
    PetscErrorCode :: ierr
    temp = 0.5d0*rho_inf*mach*mach
    h = 0.d0
    v = 0.d0
    DO j=1,shape_points
      m = shape_points_index(j)
      r = point%right(m)
      l = point%left(m)
      lx = point%x(l)
      ly = point%y(l)
      mx = point%x(m)
      my = point%y(m)
      rx = point%x(r)
      ry = point%y(r)
      ds1 = (mx-lx)**2 + (my-ly)**2
      CALL PUSHREAL8(ds1)
      ds1 = DSQRT(ds1)
      ds2 = (rx-mx)**2 + (ry-my)**2
      CALL PUSHREAL8(ds2)
      ds2 = DSQRT(ds2)
      CALL PUSHREAL8(ds)
      ds = 0.5d0*(ds1+ds2)
      nx = point%nx(m)
      ny = point%ny(m)
      CALL PUSHREAL8(cp)
      cp = point%prim(4, m) - pr_inf
      cp = -(cp/temp)
      h(point%flag_2(m)) = h(point%flag_2(m)) + cp*nx*ds
      v(point%flag_2(m)) = v(point%flag_2(m)) + cp*ny*ds
    END DO
    lcl = v*DCOS(theta) - h*DSIN(theta)
    lcd = h*DCOS(theta) + v*DSIN(theta)
    call MPI_Reduce(lCl, Cl , shapes, MPI_DOUBLE, MPI_SUM, 0, &
             & PETSC_COMM_WORLD, ierr)
    call MPI_Reduce(lCd, Cd , shapes, MPI_DOUBLE, MPI_SUM, 0, &
             & PETSC_COMM_WORLD, ierr)
    call MPI_Bcast(cl, 1, MPI_DOUBLE, 0, PETSC_COMM_WORLD, &
                  ierr)
    call MPI_Bcast(cd, 1, MPI_DOUBLE, 0, PETSC_COMM_WORLD, &
                  ierr)
    pitch_momb = 0.0_8
    pitch_momb = cmb
    clb = clb + clcdb/cd
    cdb = cdb - cl*clcdb/cd**2
    hb = 0.0_8
    vb = 0.0_8
    hb = DCOS(theta)*cdb - DSIN(theta)*clb
    vb = DCOS(theta)*clb + DSIN(theta)*cdb
    DO j=shape_points,1,-1
      m = shape_points_index(j)
      my = point%y(m)
      nx = point%nx(m)
      ny = point%ny(m)
      mx = point%x(m)
      tempb = -(ds*(mx-0.25d0)*pitch_momb(point%flag_2(m)))
      tempb0 = -(cp*ny*pitch_momb(point%flag_2(m)))
      tempb1 = ds*my*pitch_momb(point%flag_2(m))
      tempb2 = cp*nx*pitch_momb(point%flag_2(m))
      cpb = ds*ny*vb(point%flag_2(m)) + ds*nx*hb(point%flag_2(m)) + nx*&
&       tempb1 + ny*tempb
      nyb = ds*cp*vb(point%flag_2(m)) + cp*tempb
      dsb = cp*ny*vb(point%flag_2(m)) + cp*nx*hb(point%flag_2(m)) + my*&
&       tempb2 + (mx-0.25d0)*tempb0
      mxb = ds*tempb0
      nxb = ds*cp*hb(point%flag_2(m)) + cp*tempb1
      myb = ds*tempb2
      cpb = -(cpb/temp)
      CALL POPREAL8(cp)
      pointb%prim(4, m) = pointb%prim(4, m) + cpb
      pointb%ny(m) = pointb%ny(m) + nyb
      pointb%nx(m) = pointb%nx(m) + nxb
      CALL POPREAL8(ds)
      ds1b = 0.5d0*dsb
      ds2b = 0.5d0*dsb
      r = point%right(m)
      rx = point%x(r)
      ry = point%y(r)
      CALL POPREAL8(ds2)
      IF (ds2 .EQ. 0.0) THEN
        ds2b = 0.0
      ELSE
        ds2b = ds2b/(2.D0*DSQRT(ds2))
      END IF
      tempb3 = 2*(rx-mx)*ds2b
      tempb4 = 2*(ry-my)*ds2b
      rxb = tempb3
      ryb = tempb4
      l = point%left(m)
      lx = point%x(l)
      ly = point%y(l)
      CALL POPREAL8(ds1)
      IF (ds1 .EQ. 0.0) THEN
        ds1b = 0.0
      ELSE
        ds1b = ds1b/(2.D0*DSQRT(ds1))
      END IF
      tempb5 = 2*(mx-lx)*ds1b
      mxb = mxb + tempb5 - tempb3
      tempb6 = 2*(my-ly)*ds1b
      myb = myb + tempb6 - tempb4
      lxb = -tempb5
      lyb = -tempb6
      pointb%y(r) = pointb%y(r) + ryb
      pointb%x(r) = pointb%x(r) + rxb
      pointb%y(m) = pointb%y(m) + myb
      pointb%x(m) = pointb%x(m) + mxb
      pointb%y(l) = pointb%y(l) + lyb
      pointb%x(l) = pointb%x(l) + lxb
    END DO
  END SUBROUTINE COMPUTE_CL_CD_CM_B

  SUBROUTINE COMPUTE_CL_CD_CM()
    IMPLICIT NONE
    INTEGER :: i, j, k
    INTEGER :: l, m, r
    REAL*8 :: cp, temp
    REAL*8 :: lx, ly, mx, my, rx, ry
    REAL*8 :: ds1, ds2, ds
    REAL*8, DIMENSION(shapes) :: h, v, pitch_mom
    REAL*8 :: nx, ny
    INTRINSIC DSQRT
    INTRINSIC DCOS
    INTRINSIC DSIN
    temp = 0.5d0*rho_inf*mach*mach
    h = 0.d0
    v = 0.d0
    pitch_mom = 0.0d0
    DO j=1,shape_points
      m = shape_points_index(j)
      r = point%right(m)
      l = point%left(m)
      lx = point%x(l)
      ly = point%y(l)
      mx = point%x(m)
      my = point%y(m)
      rx = point%x(r)
      ry = point%y(r)
      ds1 = (mx-lx)**2 + (my-ly)**2
      ds1 = DSQRT(ds1)
      ds2 = (rx-mx)**2 + (ry-my)**2
      ds2 = DSQRT(ds2)
      ds = 0.5d0*(ds1+ds2)
      nx = point%nx(m)
      ny = point%ny(m)
      cp = point%prim(4, m) - pr_inf
      cp = -(cp/temp)
      h(point%flag_2(m)) = h(point%flag_2(m)) + cp*nx*ds
      v(point%flag_2(m)) = v(point%flag_2(m)) + cp*ny*ds
      pitch_mom(point%flag_2(m)) = pitch_mom(point%flag_2(m)) + (-(cp*ny&
&       *ds*(mx-0.25d0))+cp*nx*ds*my)
    END DO
    cl = v*DCOS(theta) - h*DSIN(theta)
    cd = h*DCOS(theta) + v*DSIN(theta)
    clcd = cl/cd
    cm = pitch_mom
  END SUBROUTINE COMPUTE_CL_CD_CM

END MODULE COMPUTE_FORCE_COEFFS_MOD_DIFF

